#!/bin/tcsh

foreach n (`find . -name "*.shp"`)
    set base_name = `basename "$n:r"`
    echo "*******************************"
    echo
    echo "$base_name"
    echo
    ogrinfo -so "$n" "$base_name"
    echo "*******************************"
    echo
end
