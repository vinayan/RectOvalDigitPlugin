#!/usr/bin/env python
#-*- coding:utf-8 -*-

def inverse(ch):
    "Retourne la chaîne inversée de ch"
    a = len(ch)
    str = ''
    while a >= 0:
        str += ch[a]
        a -= 1
    return str


print inverse('Ceci est un test.')
