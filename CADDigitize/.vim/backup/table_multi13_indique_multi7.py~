#!/usr/bin/env python
#-*- coding:utf-8 -*-

a,b = 0,7                   # a sert de compteur ; b de multiplicateur
while a < 20:
    a += 1                  # Incrementation du compteur
    if ( ((a*b)%3) == 0):   # Si le résultat de la multiplication est divisible par 3
        print a*b,"*"       # Alors on affiche une "*"
    else:
        print a*b           # Sinon on affiche seulement le résultat

